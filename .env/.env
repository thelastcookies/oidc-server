# 项目通用环境变量设置
# 端口号
PORT=8190
# OIDC Issuer URL
OIDC_ISSUER=http://localhost:8190
# SSO 登录页 URL（前后端分离时，Vue 登录页的完整地址）
SSO_LOGIN_PAGE=http://localhost:5173/login
# Cookie 签名密钥（多个用逗号分隔）
COOKIE_KEYS=oidc-cookie-key-1,oidc-cookie-key-2
# 登出后重定向地址
POST_LOGOUT_REDIRECT_URI=http://localhost:3000
